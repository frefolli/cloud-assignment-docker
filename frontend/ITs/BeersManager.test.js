import "@testing-library/jest-dom/extend-expect";
import { SETTINGS_ENDPOINT } from "../src/utils/Protocol";
import BeersManager from "../src/utils/BeersManager";

const beersManager = new BeersManager();
describe("BeersManager tests", () => {
    test("BeersManager should be able to getBeerList", () => {})
    test("BeersManager should be able to getBeer", () => {})
    test("BeersManager should be able to postBeer", () => {})
    test("BeersManager should be able to putBeer", () => {})
    test("BeersManager should be able to deleteBeer", () => {})
    test("BeersManager should be able to postNote", () => {})
    test("BeersManager should be able to putNote", () => {})
    test("BeersManager should be able to deleteNote", () => {})
})
